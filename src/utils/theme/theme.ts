import colors from '@utils/colors/colors';
import boxShadows from '@utils/shadows/box-shadows';

import {texts} from '../../atoms/typography/text';

const theme = {
  boxShadows,
  colors,
  typography: texts,
};

export default theme;
